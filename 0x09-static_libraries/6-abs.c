#include "main.h"

/**
 * _abs - A program that computes the absolute value of an integer
 * @n: the int to be checked
 * Return: Always the absolute value of the int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
